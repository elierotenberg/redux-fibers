{
  "name": "redux-fibers",
  "version": "1.0.0",
  "description": "Managed fibers/coroutine for Redux",
  "main": "dist/index.js",
  "repository": "git@github.com:elierotenberg/redux-fibers",
  "author": "Elie Rotenberg <elie@rotenberg.io>",
  "license": "MIT",
  "scripts": {
    "make:clean": "rm -rf dist",
    "make:compile": "mkdir dist && cp -r src/* dist && babel src -d dist --source-maps",
    "make:all": "yarn run make:clean && yarn run make:compile"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-eslint": "^7.2.3",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.0",
    "eslint": "^4.2.0",
    "eslint-plugin-prettier": "^2.1.2",
    "prettier": "^1.5.3"
  },
  "dependencies": {
    "babel-runtime": "^6.23.0"
  }
}
